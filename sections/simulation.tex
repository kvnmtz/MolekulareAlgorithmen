\section{Simulationsstudien}

Zur Überprüfung des chemischen Analogcomputermodells wurden zwei
Simulationsstudien durchgeführt. In der ersten Studie wird untersucht, ob
das Modell für verschiedene Eingabewerte \(s\) die korrekten stationären
Ausgabewerte berechnet. In der zweiten Studie wird bestimmt, nach welcher
Modellzeit alle relevanten Ausgabespezies eine Genauigkeit von mindestens
\(99.9\,\%\) erreicht haben.

Alle Simulationen wurden als deterministische Zeitverläufe in COPASI
durchgeführt. Die Eingabe wurde jeweils durch die Konzentration der
Spezies \(S\) kodiert. Die Spezies \(Q\), \(F\) und \(\Theta_n\) wurden mit
Anfangskonzentration \(0\) initialisiert.

\subsection{Studie 1: Validierung der Berechnung für verschiedene Eingaben}

In der ersten Simulationsstudie wurde überprüft, ob das Modell für verschiedene Werte der Eingabespezies \(S\) die erwarteten stationären Werte liefert. Dazu wurde ein Parameter-Scan über die Anfangskonzentration von \(S\) durchgeführt. Für jeden Eingabewert wurde ein Zeitverlauf bis zur Modellzeit $T=30$ mit einer Intervallgröße von $\Delta t = 0.1$ und $300$ Intervallen simuliert. Die im Modell berechneten Konzentrationen besitzen folgende Bedeutung: Die Spezies \(Q\) soll gegen den Quadratwert der Eingabe konvergieren,

\begin{equation}
    [Q]^* = s^2.
\end{equation}

Die Spezies \(F\) repräsentiert den maximalen Flächeninhalt und soll gegen

\begin{equation}
    [F]^*
    =
    A_{\max}(s)
    =
    \frac{\pi+\sqrt{\pi^2+4}}{4}s^2
\end{equation}

konvergieren. Die Spezies \(\Theta_n\) kodiert den normierten optimalen
Winkel. Da der optimale Winkel unabhängig von \(s\) ist, ist auch der
Zielwert von \(\Theta_n\) für alle Eingabewerte gleich:

\begin{equation}
    [\Theta_n]^*
    =
    \frac{
        \pi-\arctan\left(\frac{2}{\pi}\right)
    }{\pi}
    \approx
    0.819546.
\end{equation}

Am Ende der Simulation wurden die Werte der Spezies \(Q\), \(F\) und
\(\Theta_n\) mit den theoretisch erwarteten Gleichgewichtswerten
verglichen. Die Ergebnisse des Parameter-Scans sind in Tabelle
\ref{tab:studie1_parameter_scan} dargestellt. Die Simulationsergebnisse zeigen, dass alle berechneten Werte der Spezies \(Q\), \(F\) und \(\Theta_n\) mit den theoretisch erwarteten Gleichgewichtswerten übereinstimmen.

\begin{table}[h]
    \centering
    \small
    \begin{tabular}{c|c|c|c|c|c|c}
        \(s=[S]_0\) &
        \([Q](30)\) &
        \([Q]^*\) &
        \([F](30)\) &
        \([F]^*=A_{\max}(s)\) &
        \([\Theta_n](30)\) &
        \([\Theta_n]^*\) \\
        \hline
        0.5 & 0.25 & 0.25 & 0.429112 & 0.429112 & 0.819546 & 0.819546 \\
        1   & 1    & 1    & 1.71645  & 1.71645  & 0.819546 & 0.819546 \\
        2   & 4    & 4    & 6.86578  & 6.86578  & 0.819546 & 0.819546 \\
        3   & 9    & 9    & 15.4480  & 15.4480  & 0.819546 & 0.819546 \\
        5   & 25   & 25   & 42.9112  & 42.9112  & 0.819546 & 0.819546 \\
    \end{tabular}
    \caption{Endwerte des Parameter-Scans zur Modellzeit \(t=30\) im Vergleich mit den theoretischen Gleichgewichtswerten.}
    \label{tab:studie1_parameter_scan}
\end{table}



\subsection{Studie 2: Bestimmung der chemischen Rechenzeit}

In der zweiten Simulationsstudie wurde untersucht, nach welcher Modellzeit
alle relevanten Ausgabespezies eine Genauigkeit von \(99.9\,\%\) erreichen.
Dazu wurde exemplarisch der Eingabewert $s=2$ verwendet. Für diesen Eingabewert gelten die Zielwerte $[Q]^*=4$, $[F]^* \approx 6.865784432$ und $[\Theta_n]^* \approx 0.819546$ wie in Tabelle \ref{tab:studie1_parameter_scan} bereits dargestellt. Ein Ergebnis gilt als mit \(99.9\,\%\) Genauigkeit vorliegend, wenn der relative Fehler höchstens \(0.001\) beträgt. Für eine Ausgabespezies \(X\) mit Zielwert \(X^*\) bedeutet dies

\begin{equation}
    \frac{|X^* - X(t)|}{X^*}
    \leq
    0.001.
\end{equation}

Da alle betrachteten Spezies monoton von \(0\) gegen ihren Zielwert
konvergieren, kann äquivalent geprüft werden, ob

\begin{equation}
    X(t)\geq 0.999X^*
\end{equation}

gilt. 

Tabelle \ref{tab:studie2_rechenzeit} zeigt für jede Spezies jeweils den exakten Zielwert \(X^*\), den Wert, der mindestens vorliegen muss um eine Genauigkeit von \(99.9\,\%\) zu erreichen, sowie die erste Modellzeit, zu der dieser Wert erreicht wurde exemplarisch für \(s=2\). 

\begin{table}[h]
    \centering
    \begin{tabular}{c|c|c|c}
        Spezies &
        Zielwert \(X^*\) &
        \(99.9\,\%\)-Schwelle  &
        erste beobachtete Modellzeit \\

        \hline
        \(Q\)        & 4           & 3.996       & 7.0 \\
        \(F\)        & 6.865784432 & 6.858918648 & 9.3 \\
        \(\Theta_n\) & 0.819546    & 0.818727    & 7.0 \\
    \end{tabular}
    \caption{Bestimmung der Modellzeit bis zum Erreichen einer Genauigkeit von \(99.9\,\%\) für \(s=2\).}
    \label{tab:studie2_rechenzeit}
\end{table}

Die simulativ beobachtete Rechenzeit kann anhand der Differentialgleichung
für \(Q\) nachvollzogen werden. Für \(s=2\) gilt im stationären Zustand
$[Q]^* = s^2 = 4$. Die Dynamik von \(Q\) ist durch

\begin{equation}
    \frac{d[Q]}{dt}
    =
    [Q]^*-[Q]
\end{equation}

Da es sich hierbei um eine lineare Differentialgleichung erster Ordnung
mit konstantem Zielwert \([Q]^*\) handelt und \(Q\) mit der
Anfangskonzentration \([Q](0)=0\) initialisiert wird, ergibt sich die
Lösung
\begin{equation}
    [Q](t)
    =
    [Q]^*
    \left(
        1-e^{-t}
    \right).
    \label{eq:Q_loesung}
\end{equation}

Zur Bestimmung der Modellzeit bis \(99.9\,\%\) Genauigkeit wird
\eqref{eq:Q_loesung} in die Bedingung

\begin{equation}
    [Q](t)\geq0.999[Q]^*
\end{equation}

eingesetzt. Dadurch folgt

\begin{equation}
    [Q]^*
    \left(
        1-e^{-t}
    \right)
    \geq
    0.999[Q]^*.
\end{equation}

Da \([Q]^*>0\) gilt, kann durch \([Q]^*\) dividiert werden:

\begin{equation}
    1-e^{-t}
    \geq
    0.999.
\end{equation}

Umstellen liefert

\begin{equation}
    e^{-t}
    \leq
    0.001.
\end{equation}

Durch Logarithmieren erhält man

\begin{equation}
    t
    \geq
    \ln(1000).
\end{equation}

Damit ergibt sich

\begin{equation}
    t
    \geq
    6.91.
\end{equation}

Für \(Q\) wird theoretisch also erwartet, dass die
\(99.9\,\%\)-Schwelle nach etwa \(6.91\) Modellzeiteinheiten erreicht wird.
Die in Tabelle \ref{tab:studie2_rechenzeit} beobachtete erste Modellzeit
von \(t=7.0\) stimmt damit überein. Die leichte Abweichung ergibt sich
daraus, dass der simulierte Zeitverlauf mit einer endlichen Schrittweite
ausgewertet wurde. Analog dazu könnte auch für Spezies \(F\) und \(\Theta_n\) die theoretische Rechenzeit bestimmt werden, aus didaktischen Gründen wird dies hier jedoch nicht weiter ausgeführt.
